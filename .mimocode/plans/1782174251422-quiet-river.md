# Plan: Restore v34 Network Config + Create VERSION_CHANGELOG.md

## Exact v34 Networking Config (from backup)

### behaviour.rs — Gossipsub (line 119-124)
```rust
let gossipsub_config = gossipsub::ConfigBuilder::default()
    .heartbeat_interval(std::time::Duration::from_secs(10))
    .validation_mode(gossipsub::ValidationMode::Strict)
    .message_id_fn(message_id_fn)
    .build()
    .expect("Valid gossipsub config");
```
**No `.max_transmit_size()` call** — unlimited message size.

### behaviour.rs — Request-response codec (line 59-61)
```rust
.set_request_size_maximum(10 * 1024 * 1024)  // 10MB
.set_response_size_maximum(10 * 1024 * 1024) // 10MB
```

### behaviour.rs — Relay config (line 93-98)
```rust
max_circuit_bytes: 1024 * 1024 * 1024,  // 1GB
max_circuit_duration: std::time::Duration::from_secs(60 * 60),  // 1 hour
max_reservations: 8192,
max_circuits: 4096,
```

## Current (v35) Values to Revert

| Line | Current (v35) | Restore to (v34) |
|------|--------------|-----------------|
| 60 | `2 * 1024 * 1024` (2MB) | `10 * 1024 * 1024` (10MB) |
| 61 | `2 * 1024 * 1024` (2MB) | `10 * 1024 * 1024` (10MB) |
| 94 | `100 * 1024 * 1024` (100MB) | `1024 * 1024 * 1024` (1GB) |
| 95 | `30 * 60` (30 min) | `60 * 60` (1 hour) |
| 96 | `256` | `8192` |
| 97 | `100` | `4096` |
| 120 | `from_secs(30)` | `from_secs(10)` |
| 123 | `.max_transmit_size(1024 * 1024)` | **Remove this line** |

## Files to Modify
1. `src/network/behaviour.rs` — Apply all 8 changes above
2. `for_linux/src/network/behaviour.rs` — Same changes for RBN
3. `VERSION_CHANGELOG.md` — Create in project root (content below)

## VERSION_CHANGELOG.md Content
Create at `/Users/dev/Development/introvert/VERSION_CHANGELOG.md` with the version table from v27-v36, key networking parameters table, and lessons learned.

## Rebuild & Deploy
1. `make mac` — rebuild macOS
2. `bash deploy_rbn.sh` — redeploy RBN
3. `flutter run -d macos` — test

## Verification
- Send group message → receiver gets Type=21 event in real-time
- Direct P2P file transfer still works at 70+ Mbps
- RBN logs show gossipsub message forwarding
